'use strict';

/**
 * dashboard-metric service
 * VERSÃO 2 - STANDBY
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dashboard-metric.dashboard-metric');
