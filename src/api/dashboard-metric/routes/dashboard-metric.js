'use strict';

/**
 * dashboard-metric router
 * VERSÃO 2 - STANDBY
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::dashboard-metric.dashboard-metric');
