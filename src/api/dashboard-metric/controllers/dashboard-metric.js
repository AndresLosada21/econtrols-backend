'use strict';

/**
 * dashboard-metric controller
 * VERSÃO 2 - STANDBY
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::dashboard-metric.dashboard-metric');
